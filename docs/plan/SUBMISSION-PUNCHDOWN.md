# Final submission punch-down

**Date:** 2026-06-14 · **Branch:** `claude/microfactory-gradio-hackathon-9e81fh`
**Space:** `build-small-hackathon/microfactory-lab`

The single authoritative list for shipping. Supersedes the older `SUBMISSION-AUDIT.md`
(archived). Operational seed/deploy steps live in
[`FINAL-SEED-AND-DEPLOY.md`](FINAL-SEED-AND-DEPLOY.md); badge eligibility detail in
[`writeup/03-SUBMISSION-CHECKLIST.md`](writeup/03-SUBMISSION-CHECKLIST.md).

---

## A · Done & verified (in the repo)

- [x] App builds; `make test` (test_core.py) all green.
- [x] Live deliberation logging wired into every run (`core/deliberation_log.py`),
      `HF_TOKEN`-gated + crash-safe, same contract as the field log.
- [x] Deliberation static export (`make deliberation`) + docs; shares one schema with the live log.
- [x] Ledger deduped (3 exact-duplicate rows removed → 12 seed + 11 ingested = 23 unique).
- [x] Deploy hygiene: `.agents/`, `.codeboarding/`, `deliberation_logs/` excluded from the Space upload.
- [x] README final tags (track/sponsor/achievement) + all three dataset links; `short_description` ≤60.
- [x] Dataset cards ready: `reference/dataset-cards/ledger-README.md`, `field-log-README.md`;
      deliberation card auto-generated by `make deliberation`.
- [x] No secrets/PII/local-paths/internal model IDs in any shipped file (audited 2026-06-14).

## B · Your punch list (ordered — do locally; needs HF write token + `ollama serve`)

1. [x] **Seed the 3 datasets** — `make trace` (post-dedup), `make deliberation` (Ollama up for real
       reasoning), upload the field-log card. Exact commands: `FINAL-SEED-AND-DEPLOY.md` §1.
       *Completed 2026-06-14: ledger + deliberation traces regenerated and uploaded; field-log card already present.*
2. [x] **Set `HF_TOKEN` as a Space secret** — one secret turns on BOTH live logs (field + deliberation). §2.
       *Completed 2026-06-14 via `hf spaces secrets add build-small-hackathon/microfactory-lab`.*
3. [x] **Capture screenshots** → `assets/screenshots/` (`hero-build.png`, `print-loop.png`,
       `second-opinion.png`; the last doubles as Off-Brand visual evidence).
       *Completed 2026-06-15: captured from the running app at 1500px (2×) and committed. Shot in
       deterministic-fallback mode (no Ollama); re-shoot from the live Space with real Gemma is
       optional for richer on-screen reasoning text.*
4. [ ] **Record + upload the demo video** (see `writeup/07-VIDEO-PRODUCTION-PROMPT.md`), then paste
       the URL into the README Links "Demo video" placeholder.
5. [ ] **Publish the social post**, then paste the URL into the README Links "Social post" placeholder.
6. [ ] **Publish Field Notes on the org blog** (`writeup/06-FIELD-NOTES.md`) — required for that badge.
7. [ ] **Redeploy the Space** — `make deploy-check` → `make deploy` so the live README/code/links reflect all the above. §3.
8. [ ] **Submit** on the HF portal: https://build-small-hackathon-field-guide.hf.space/submit
9. [ ] **Post in the Discord group.**

## C · Verify after deploy (5 min)

- [ ] Live UI smoke test: BUILD shows O'Brien reasoning (not "Error"); La Forge second opinion +
      dispute gate; LAYER scrubber; Print loop climbs; Review shows ledger + verdict + ↺ RESET; wide layout.
- [ ] Drive one BUILD→PRINT on the Space; wait ≤5 min; confirm new rows in BOTH
      `interactions.jsonl` (field log) and `deliberations.jsonl` (deliberation).
- [ ] All three dataset pages render a clean table in the HF viewer; cards show as each repo README.

## D · Claim-integrity watch (frontmatter pre-claims these — finish backing them before judging)

- `sharing-is-caring` — ✅ earned now (ledger published); deliberation + field log are bonus once seeded.
- `field-notes` — needs B6 (blog published).
- `off-brand` — needs B3 (screenshot evidence).
- `tiny-titan` — **confirm the org ruling** on E-model param counting; per your own checklist, drop the
  tag if unfavorable. (You chose to keep it — verify before submit.)
- `well-tuned` — rests on the "end-to-end system tuning" framing; the LoRA is the weights-level version (training).

## E · Open decisions

- [ ] **Promote the 3 `modal-saviour-*` candidate lessons** (`data/_modal_candidate_lessons.jsonl`) into
      the shared ledger before `make trace`, or keep them staged (preserves the human-reviewed-only gate)?
- [ ] Confirm the exact **submission deadline/cutoff** so B1–B9 land in time.
